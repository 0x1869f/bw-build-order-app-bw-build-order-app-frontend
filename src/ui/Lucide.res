module Ban = {
  @module("lucide-react")
  @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~color: string=?,
    ~strokeWidth: int=?,
    ~absoluteStrokeWidth: bool=?,
  ) => React.element = "Ban"
}

module Trash = {
  @module("lucide-react")
  @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~color: string=?,
    ~strokeWidth: int=?,
    ~absoluteStrokeWidth: bool=?,
  ) => React.element = "Trash"
}

module Pencil = {
  @module("lucide-react")
  @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~color: string=?,
    ~strokeWidth: int=?,
    ~absoluteStrokeWidth: bool=?,
  ) => React.element = "Pencil"
}

module Check = {
  @module("lucide-react")
  @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~color: string=?,
    ~strokeWidth: int=?,
    ~absoluteStrokeWidth: bool=?,
  ) => React.element = "Check"
}

module Download = {
  @module("lucide-react")
  @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~color: string=?,
    ~strokeWidth: int=?,
    ~absoluteStrokeWidth: bool=?,
  ) => React.element = "Download"
}

module MessageCircle = {
  @module("lucide-react")
  @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~color: string=?,
    ~strokeWidth: int=?,
    ~absoluteStrokeWidth: bool=?,
  ) => React.element = "MessageCircle"
}

module MessageCircleOff = {
  @module("lucide-react")
  @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~color: string=?,
    ~strokeWidth: int=?,
    ~absoluteStrokeWidth: bool=?,
  ) => React.element = "MessageCircleOff"
}

module Plus = {
  @module("lucide-react")
  @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~color: string=?,
    ~strokeWidth: int=?,
    ~absoluteStrokeWidth: bool=?,
  ) => React.element = "Plus"
}

module FileUp = {
  @module("lucide-react")
  @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~color: string=?,
    ~strokeWidth: int=?,
    ~absoluteStrokeWidth: bool=?,
  ) => React.element = "FileUp"
}

module UserRoundPen = {
  @module("lucide-react")
  @react.component
  external make: (
    ~className: string=?,
    ~size: int=?,
    ~color: string=?,
    ~strokeWidth: int=?,
    ~absoluteStrokeWidth: bool=?,
  ) => React.element = "UserRoundPen"
}
