module ManageAccounts = {
  module Filled = {
    @react.component @module("@mui/icons-material/ManageAccounts")

    external make: (~color: string=?, ~fontSize: string=?) => React.element = "default"
  }
}

module Logout = {
  module Filled = {
    @react.component @module("@mui/icons-material/Logout")

    external make: (~color: string=?, ~fontSize: string=?) => React.element = "default"
  }
}

module Clear = {
  module Filled = {
    @react.component @module("@mui/icons-material/Clear")

    external make: (~color: string=?, ~fontSize: string=?) => React.element = "default"
  }
}

module Download = {
  module Filled = {
    @react.component @module("@mui/icons-material/Download")

    external make: (~color: string=?, ~fontSize: string=?) => React.element = "default"
  }
}

module Close = {
  module Filled = {
    @react.component @module("@mui/icons-material/Close")

    external make: (~color: string=?, ~fontSize: string=?) => React.element = "default"
  }
}
